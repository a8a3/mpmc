
### [Async MPMC queue](include/basic/mpmc.hpp)

Неограниченная MPMC очередь.

Содержит 2 внутренние очереди - одна для значений, другая для коллбеков. 
Коллбеки будут исполняться в потоках, вызывающих Enqueue/Dequeue.

В случае закрытия очереди (вызов `Close`) или уничтожения через деструктор, оставшиеся коллбеки вызываются со значением `std::nullopt`.

Послe закрытия очереди вызов `Enqueue` вернет `false`, вызов `Dequeue` будет вызван или с
оставшимся в очереди значений значением или с `std::nullopt` если очередь значений пуста.

Преимущества-
- простота реализации;
- поддерживается FIFO семантика очереди;
- нет накладных расходов на поддержку отдельного потока исполнения для коллбеков;

Особенности-
- коллбеки должны быть легковесными (должно быть предусмотрено пользователем);
- время жизни объекта очереди должно превышать время жизни объектов consumer/producer,
  возможно UB в случае одновременной работы деструктора очереди и вложенных внутрь коллбеков
  обращений к интерфейсу очереди;


### [Cancellable Async MPMC queue](include/cancellable/mpmc.hpp)

Асинхронная очередь с возможностью отмены ожидающих исполнения коллбеков.
Вызов Dequeue теперь возвращает опциональный идентификатор коллбека (в случае если коллбек был выполнен немедленно, возвращается `std::nullopt`)

Внутри вместо `std::queue` используется `std::unordered_map`, для доступа к коллбеку по идентификатору для отмены и `std::list`, содержащий коллбеки для исполнения.

Преимущества:
- возможность отмены коллбека, ожидающего исполнение;

Недостатки:
- накладные расходы на хранение информации о коллбеках в двух структурах данных;


### [Bounded Async MPMC queue](include/bounded/mpmc.hpp)
Ограниченная(bounded) асинхронная очередь.

Очередь параметризована целочисленным значением TCapacity.

Метод Enqueue принимает коллбек- продюсер, который возвращает значение для консьюмера. Коллбек принимает флаг, означающий закрыта очередь или нет.

Если в очереди значений достаточно места, коллбек- продюсер вызывается сразу и если он возвращает значение, оно помещается в очередь значений. 

В случае, если количество значений в очереди равно установленному TCapacity, следующие переданные в Enqueue продюсеры помещаются в отдельную очередь и достаются из нее в момент появления места в очереди значений.